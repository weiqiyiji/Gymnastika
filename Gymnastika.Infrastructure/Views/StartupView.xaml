<UserControl x:Class="Gymnastika.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
    	<Grid.RowDefinitions>
    		<RowDefinition />
    		<RowDefinition Height="Auto"/>
    	</Grid.RowDefinitions>
        <!--<Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.ColumnSpan="2">登录</Label>
            <TextBlock Text="用户名" Grid.Row="1" />
            <TextBox Grid.Column="1" Grid.Row="1" />
            
            <TextBlock Text="密码" />
        </Grid>-->    
        
        <ListBox DisplayMemberPath="UserName" HorizontalAlignment="Center" VerticalAlignment="Center" ItemsSource="{Binding RegisteredUsers}" SelectedItem="{Binding SelectedUser}" />
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Horizontal">
        	<Button Command="{Binding LogOnCommand}" Content="登录" Margin="4" />
        	<Button Content="新用户" Margin="4" Command="{Binding CreateNewUserCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
