<Window x:Class="Gymnastika.Views.LogOnView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="LogOnView" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.ColumnSpan="2">登录</Label>
            <TextBlock Text="用户名" Grid.Row="1" />
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding UserName}" />
            
            <TextBlock Text="密码" Grid.Row="2" />
            <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding Password}" />
            
            <Button Grid.Row="3" Grid.RowSpan="2" Command="{Binding LogOnCommand}" />
        </Grid>
    </Grid>
</Window>
