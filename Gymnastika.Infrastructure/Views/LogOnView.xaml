<UserControl x:Class="Gymnastika.Views.LogOnView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:Gymnastika.Controls;assembly=Gymnastika.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <controls:WatermarkTextBox Text="{Binding UserName}" WatermarkText="用户名" Style="{DynamicResource WatermarkTextBoxStyle}" />
        <controls:WatermarkTextBox Grid.Row="1" Text="{Binding Password}" WatermarkText="密码" Style="{DynamicResource WatermarkTextBoxStyle}" />
        <Button Grid.Row="2" Command="{Binding LogOnCommand}" Grid.ColumnSpan="2" Content="登录" Margin="0,10,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{DynamicResource ActionButtonStyle}" />
    </Grid>
</UserControl>
