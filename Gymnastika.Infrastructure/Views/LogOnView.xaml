<Window x:Class="Gymnastika.Views.LogOnView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behaviors="clr-namespace:Gymnastika.Common.Behaviors;assembly=Gymnastika.Common"
        Title="LogOnView" Height="300" Width="300" behaviors:CloseWindowBehavior.CloseCondition="{Binding IsSuccess}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.13*" />
            <RowDefinition Height="0.87*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="登录" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Grid Grid.RowSpan="2" Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="用户名" Grid.Row="1" />
            <TextBox Grid.Column="1" Grid.Row="1" Text="{Binding UserName}" />
            
            <TextBlock Text="密码" Grid.Row="2" />
            <TextBox Grid.Column="1" Grid.Row="2" Text="{Binding Password}" />
            
            <Button Grid.Row="3" Command="{Binding LogOnCommand}" Grid.ColumnSpan="2" Content="登录" />
        </Grid>
    </Grid>
</Window>
