<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gymnastika.Modules.Sports.Views"
             xmlns:converters="clr-namespace:Gymnastika.Modules.Sports.Converters" 
    xmlns:controls="clr-namespace:Gymnastika.Controls;assembly=Gymnastika.Controls" 
    xmlns:Custom="http://schemas.microsoft.com/surface/2008" x:Class="Gymnastika.Modules.Sports.Views.SportsPlanItemView" 
             mc:Ignorable="d"  x:Name="Root" Width="270">
    <Grid>
    	<Grid.ColumnDefinitions>
    		<ColumnDefinition Width="0.276*"/>
    		<ColumnDefinition Width="0.724*"/>
    	</Grid.ColumnDefinitions>
    	<TextBlock x:Name="Time" Text="{Binding Time}"/>
		<TextBlock x:Name="运动名称" TextWrapping="Wrap" Text="{Binding SportName}" Grid.Column="1" />
		<Custom:SurfaceButton Content="X" Command="{Binding CancelCommand}" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top" Height="26" Width="37.21" Background="{x:Null}"/>
        <!--<local:PlanItemDetailView x:Name="planItemDetailView" Height="{Binding Height}" HorizontalAlignment="Right" Width="0">
            <local:PlanItemDetailView.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform/>
                </TransformGroup>
            </local:PlanItemDetailView.RenderTransform>
        </local:PlanItemDetailView>
    	<Custom:SurfaceButton x:Name="Close" Command="{Binding CancelCommand}" Content="X" Margin="0,1,0,0" Width="18.877" RenderTransformOrigin="0.355,0.317" d:LayoutOverrides="VerticalAlignment" Height="20.761" VerticalAlignment="Top"/>-->
	</Grid>
</UserControl>
