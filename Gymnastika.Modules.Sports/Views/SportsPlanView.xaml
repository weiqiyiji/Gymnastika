<UserControl x:Class="Gymnastika.Modules.Sports.Views.SportsPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="361" d:DesignWidth="135"
             xmlns:dd="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop" Name="root">
	<UserControl.Resources>
		<DataTemplate x:Key="PlanListItemTemplate">
			<Border BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Stretch" Width="267" Height="59.833">
				<Grid Margin="-1">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="0.236*"/>
						<ColumnDefinition Width="0.61*"/>
						<ColumnDefinition Width="0.154*"/>
					</Grid.ColumnDefinitions>
					<Grid.RowDefinitions>
						<RowDefinition Height="*"/>
					</Grid.RowDefinitions>
					<TextBlock TextWrapping="Wrap" Text="{Binding Sport.Name}" Height="Auto" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Width="Auto"/>
					<Button Command="{Binding DataContext.CancleCommand, ElementName=root}" CommandParameter="{Binding}" Content="Cancle" HorizontalAlignment="Stretch" Height="Auto" Margin="0" VerticalAlignment="Stretch" Width="Auto" Grid.Column="2"/>
					<TextBlock Grid.Column="1" Margin="0" TextWrapping="Wrap" Text="" VerticalAlignment="Stretch"/>
				</Grid>
			</Border>
		</DataTemplate>
	</UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44*" />
            <RowDefinition Height="317*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="textBlock1" Text="我的计划" />
        <ListBox HorizontalContentAlignment="Stretch" dd:DragDrop.IsDropTarget="True" dd:DragDrop.DropHandler="{Binding}" SelectedItem="{Binding SelectedPlanItem}" dd:DragDrop.IsDragSource="True" dd:DragDrop.DragHandler="{Binding}" ItemTemplate="{DynamicResource PlanListItemTemplate}" ItemsSource="{Binding SportsPlan.SportsPlanItems,Mode=TwoWay}" Grid.Row="1" />
    </Grid>
</UserControl>
