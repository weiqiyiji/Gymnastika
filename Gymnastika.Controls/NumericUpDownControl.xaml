<UserControl x:Class="Gymnastika.Controls.NumericUpDownControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="37" d:DesignWidth="124">
    <UserControl.Resources>

        <SolidColorBrush x:Key="GreenBackground" Color="#94E0B1" />
        <SolidColorBrush x:Key="GrayText" Color="#7D846C" />
    </UserControl.Resources>
    <Border CornerRadius="0" BorderThickness="1" BorderBrush="{StaticResource GrayText}" Background="White" Name="rootBorder">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="0" Height="Auto" Name="lblValue" Foreground="{StaticResource GrayText}" />
    </Grid>
    </Border>
</UserControl>
